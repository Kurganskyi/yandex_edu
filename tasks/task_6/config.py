"""
Конфигурация для телеграм-бота
"""

import os
from typing import Optional

# Получение токена из переменной окружения или использование значения по умолчанию
BOT_TOKEN: str = os.getenv("BOT_TOKEN", "YOUR_BOT_TOKEN_HERE")

# Настройки логирования
LOG_LEVEL: str = os.getenv("LOG_LEVEL", "INFO")
LOG_FORMAT: str = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# Настройки таймеров
MAX_TIMER_DURATION: int = 24 * 60 * 60  # 24 часа в секундах
MIN_TIMER_DURATION: int = 1  # 1 секунда

# Настройки бота
BOT_USERNAME: Optional[str] = None  # Будет установлен автоматически при запуске

# Сообщения бота
MESSAGES = {
    "welcome": (
        "👋 Привет! Я бот для управления таймерами.\n\n"
        "📋 Доступные команды:\n"
        "• /timer <время> - установить таймер\n"
        "• /cancel - отменить активный таймер\n"
        "• /help - справка по использованию\n\n"
        "⏰ Примеры использования:\n"
        "• /timer 10m - таймер на 10 минут\n"
        "• /timer 30s - таймер на 30 секунд\n"
        "• /timer 2h - таймер на 2 часа"
    ),
    "help": (
        "📖 Справка по использованию бота\n\n"
        "🔧 Команды:\n"
        "• /timer <время> - установить таймер\n"
        "• /cancel - отменить активный таймер\n"
        "• /start - показать приветствие\n"
        "• /help - показать эту справку\n\n"
        "⏰ Форматы времени:\n"
        "• s - секунды (1s - 3600s)\n"
        "• m - минуты (1m - 1440m)\n"
        "• h - часы (1h - 24h)\n\n"
        "📝 Примеры:\n"
        "• /timer 5m - таймер на 5 минут\n"
        "• /timer 30s - таймер на 30 секунд\n"
        "• /timer 1h - таймер на 1 час\n\n"
        "⚠️ Ограничения:\n"
        "• Один активный таймер на пользователя\n"
        "• Таймеры хранятся в памяти\n"
        "• При перезапуске бота таймеры сбрасываются"
    ),
    "timer_set": "✅ Таймер на {time} установлен",
    "timer_cancelled": "✅ Таймер отменён",
    "no_timer": "ℹ️ Нет активного таймера",
    "time_up": "⏰ Время вышло!",
    "invalid_format": (
        "❌ Неверный формат команды.\n"
        "Используйте: /timer <время>\n"
        "Пример: /timer 10m"
    ),
    "invalid_time": (
        "❌ Неверный формат времени.\n"
        "Используйте: /timer <время>\n"
        "Примеры: /timer 10m, /timer 30s, /timer 2h\n\n"
        "📋 Поддерживаемые форматы:\n"
        "• s - секунды (1-3600)\n"
        "• m - минуты (1-1440)\n"
        "• h - часы (1-24)"
    ),
    "timer_error": "❌ Ошибка при установке таймера. Попробуйте еще раз.",
    "unknown_command": (
        "❓ Неизвестная команда.\n"
        "Используйте /help для просмотра доступных команд."
    ),
    "no_token": (
        "❌ Ошибка: Необходимо указать токен бота в переменной BOT_TOKEN\n"
        "Получите токен у @BotFather в Telegram"
    )
}
